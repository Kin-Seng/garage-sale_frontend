import React from 'react';

const Loading = (props) => {
    
    return(
        <img src={props.imgSrc} 
        className="centered"
        alt={{}}>
        </img>
        
    )
}

export default Loading;